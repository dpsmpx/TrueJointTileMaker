# True Joint Tile Maker

Редактор пиксельной графики и тайлсетов на PascalABC.NET.

## Требования

- Windows с .NET Framework 4.x
- [PascalABC.NET](http://pascalabc.net/) для сборки из исходников

## Сборка и запуск

Откройте `TJTM.pas` в среде PascalABC.NET и нажмите F9. Модули `TJTMColor.pas`
и `TJTMConfig.pas` лежат рядом и компилируются автоматически.

Из командной строки:

```
pabcnetcclear.exe TJTM.pas
```

Рядом с исполняемым файлом должны находиться:

| Путь | Назначение |
| --- | --- |
| `Config.txt` | настройки; создаётся автоматически, если файла нет |
| `Resources/` | значки инструментов и палитра по умолчанию |
| `Картинки/` | стартовое изображение `debug.png` |

Отсутствие любого из них не мешает запуску: значки заменяются подписями,
палитра — радужной заготовкой, а вместо стартового изображения создаётся
пустой тайл размера из настроек.

## Структура

| Файл | Содержимое |
| --- | --- |
| `TJTM.pas` | состояние документа, отрисовка, ввод, экраны интерфейса |
| `TJTMColor.pas` | преобразования HSV и RGB, смешивание с прозрачностью, растровые буферы |
| `TJTMConfig.pas` | чтение и запись `Config.txt` |

## Управление

Правило кнопок мыши одинаково для холста, палитры и тайлсета:

- **левая** применяет: рисует, заливает, кладёт цвет или тайл в ячейку;
- **правая** берёт: работает пипеткой, забирает цвет или тайл из ячейки;
- **нажатие на ролик** выполняет служебное действие: меняет местами рабочие
  цвета, а на палитре и тайлсете открывает загрузку и сохранение.

Вращение ролика меняет масштаб.

| Клавиша | Действие |
| --- | --- |
| `Escape` | главное меню |
| `N` / `L` / `S` | создать, загрузить, сохранить |
| `A` | сохранить выделение или буфер отдельным файлом |
| `Z` / `Y` | отменить и повторить |
| `V` | вставить буфер в позицию курсора |
| `X` | поменять местами первый и второй цвета |
| `P` | точный выбор цвета по каналам ARGB |
| `G` / `C` | сетка, шахматка под прозрачными пикселями |
| `R` / `Delete` | случайные цвета, очистить тайл |
| `1` `2` `3` | карандаш, заливка, выделение |
| `+` `−` | масштаб |
| стрелки | сдвиг изображения |
| `Пробел` | вернуть изображение в центр |
| `H` | справка и благодарности |

## Настройки

Все параметры хранятся в `Config.txt` в кодировке UTF-8, по строке на параметр
в виде `ключ = значение`. Порядок строк значения не имеет, неизвестные ключи
игнорируются, а испорченный или отсутствующий файл не мешает запуску: берутся
значения по умолчанию. Файл можно править вручную или через пункт
**Настройки** в меню.

| Ключ | Значение |
| --- | --- |
| `ShowHelpOnStart` | показывать справку при запуске |
| `ShowMenuOnStart` | показывать главное меню при запуске |
| `DefaultTileWidth`, `DefaultTileHeight` | размер нового тайла, 4..512 |
| `ShowGrid` | сетка между пикселями |
| `ShowChecker` | шахматка под полупрозрачными пикселями |
| `StartupImage` | картинка, открываемая при запуске; пусто — не открывать |
| `AutoLoadLastFile` | открывать последний файл вместо стартовой картинки |
| `PaletteFile` | палитра 8×8, загружаемая при запуске |
| `LastFile`, `LastFolder` | заполняются программой |

При запуске программа пробует открыть стартовое изображение, и размер тайла
берётся из него. Размер из настроек нужен только тогда, когда открыть картинку
не удалось: тогда создаётся пустой тайл заданного размера.

## Формат тайлсета

Тайлсет сохраняется одной картинкой из 8×8 ячеек. Фон заливается служебным
цветом `RGB(1, 2, 3)`, который отмечает правый и нижний край каждого тайла
внутри его ячейки. Ширина ячейки на пиксель больше самого широкого тайла,
поэтому разделитель есть всегда.

## Лицензия

GNU General Public License версии 3, см. файл `LICENSE`.

## Благодарности

Автор — dpsmpx, [vk.com/dpsmpx](https://vk.com/dpsmpx).

Команде PascalABC.NET за язык, среду разработки и библиотеку GraphABC, на
которой построен весь интерфейс редактора.
