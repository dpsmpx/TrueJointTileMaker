# True Joint Tile Maker — файл настроек.
# Формат: ключ = значение. Строки с # или ; в начале игнорируются.
# Логические значения: да / нет.
# Файл сохраняется в кодировке UTF-8.

# Показывать справку при запуске.
ShowHelpOnStart = нет

# Показывать главное меню при запуске.
ShowMenuOnStart = да

# Размер нового тайла по умолчанию, 4..512.
# Используется только когда стартовое изображение не загрузилось.
DefaultTileWidth = 32
DefaultTileHeight = 32

# Показывать сетку между пикселями.
ShowGrid = нет

# Показывать шахматку под полупрозрачными пикселями.
ShowChecker = да

# Рисовать тайл повторяющимся, чтобы сразу видеть швы.
ShowSeamless = нет

# Запускаться сразу в режиме карты локации.
StartInMapMode = нет

# Размер новой карты локации, 4..256.
DefaultMapWidth = 48
DefaultMapHeight = 18

# Папка с картами локаций, например data/maps игры Andors-Love.
MapsFolder =

# Изображение, загружаемое при запуске. Пусто — не загружать.
# Размер тайла берётся из самого изображения.
StartupImage = Картинки/debug.png

# Открывать последний файл вместо стартового изображения.
AutoLoadLastFile = нет

# Файл палитры 8x8, загружаемый при запуске.
PaletteFile = Resources/Palitra.png

# Заполняется программой.
LastFile =
LastMapFile =
LastFolder =
